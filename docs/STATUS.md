# 执行状态

更新时间：2026-09-23。

## 已完成

- 建立独立 Node.js / SQLite 项目与移动端响应式界面。
- 实现资料导入、PDF 页面显示、PPTX 文字页、图片/PDF 矩形圈选。
- 实现课程笔记、来源定位、检索、编辑与 Markdown 导出。
- 实现手动/模型复习卡与基本间隔复习。
- 实现课堂打点、页码、图片附件、分段录音、结束课堂生成时间轴笔记。
- 实现可选模型问答、OCR、翻译、总结、代码解释、卡片与录音转写接口。
- 实现内置工具权限、流程组合/JSON 分享/运行记录、后端课堂与考试模式检查。
- 实现应用壳缓存、可选只读笔记快照、局域网访问口令。
- 实现一次性手机配对码、设备令牌和撤销、分片校验与续传、带步骤进度的持久化后台任务。
- 实现手机 IndexedDB 待同步队列、电脑身份核对、重复提交去重、笔记冲突保留以及离线课堂事件同步。
- 实现 Android 调试版 APK：复用现有界面，提供系统分享导入、用户授权单次截图、悬浮入口和通知栏前台录音。
- 增加 Tailscale Serve 私有 HTTPS 异网试跑入口；去除回环地址自动管理员权限，电脑网页改为显式口令登录。
- 本机已将 Tailscale 1.102.4 安装到 `D:\Tools\Tailscale`，官方 Windows MSI 与 Android APK 的 SHA-256 已核对；学习助手 APK 已复制到 `D:\Tools` 且与构建产物哈希一致。异网启动脚本已适配自定义安装路径，并限定仅短暂配置 Serve 的辅助进程请求 UAC。
- 新增「工作台」视图：下拉选择单个工具、各工具专属界面、信息流可增删改查；自由提问的关键词规划保持不变。
- 补齐笔记、卡片、流程、资料、课程、运行记录的编辑与删除：级联规则集中在 `lib/removal.mjs`，含离线删除/编辑的版本校验、墓碑与幂等重放。
- 完成启动说明、接口文档、开发边界和测试脚本。

## 已验证

- Node 自动测试：本地运算、流程权限、模型卡片格式、SQLite 重启持久化、真实 PPTX/PDF 解析、完整 API 闭环、访问口令、模型请求协议。
- Chrome 真实浏览器：导入资料、图片圈选、笔记编辑、卡片复习、课堂归档、自定义流程运行、模式切换；桌面、390px 手机和 820px 平板布局。
- 升级 PDF.js 到修复已知漏洞的 6.2.108 或更新兼容版本；依赖安装审计为 0 个已知漏洞。
- 11 项 Node 测试覆盖配对、设备撤销、分片续传、离线操作去重、任务恢复和离线课堂对齐。
- 浏览器模拟离线笔记、断线重连后同步，并验证局域网 HTTP 下使用的 SHA-256 实现。
- Android SDK 34 编译、D8 打包、APK 资源路径检查和 v3 签名验证通过；产物 `android/build/study-companion-debug.apk`。

## 需要真实设备/服务验收

- 用户尚未配置有效模型密钥，未调用付费模型验证翻译、OCR、视觉理解及语音质量。
- 浏览器自动化不代替手机/平板真机麦克风、相机、权限弹窗和系统后台行为验收。
- 当前没有连接 Android 设备，APK 尚未真机安装；悬浮权限、每次截图授权、后台录音与系统分享仍需设备验收。
- Tailscale 服务已安装并运行，但当前未确认账号登录；配置 Serve 时的第二次 UAC 未获批准，因此尚未生成私有 HTTPS 地址，也没有启动异网工作站。需用户完成登录、确认 UAC，再按 README 用手机移动数据试跑。
- iOS 原生接入、系统后台上传作业、完整第三方插件生态和商业化尚未实现，详见 README 的后续阶段。

本次交付范围为可运行 MVP 源代码。测试数据只使用独立临时数据库，应用正式数据保持空白，除首次创建的课程外不预置伪造记录。
